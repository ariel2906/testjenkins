pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        echo 'mensaje de bienvenida'
        sleep 2
      }
    }

    stage('build') {
      steps {
        sleep 2
      }
    }

  }
  environment {
    test = 'test'
  }
}